# Hand-written pages

Put `<version>/<page>.mdx` here to replace a converted page of one version, or `common/<page>.mdx` next to the MkDocs source `common/<page>.md` it was written for: it replaces the page in every version whose source is identical (`__VERSION__` becomes the version).
